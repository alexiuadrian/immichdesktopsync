info:
  name: Get Server Version
  type: http
  seq: 4

http:
  method: GET
  url: {{baseUrl}}/api/server-info/version
  auth:
    type: bearer
    token: {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
